apiVersion: v1
kind: Service
metadata:
  name: techchallenger-service
  namespace: techchallenger-ns
spec:
  type: LoadBalancer
  selector:
    app: techchallenger
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
