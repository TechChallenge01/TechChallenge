apiVersion: v1
kind: ConfigMap
metadata:
  name: techchallenger-config
  namespace: techchallenger-ns
data:
  APP_ENV: "production"
  APP_PORT: "8080"
  LOG_LEVEL: "info"
  ASPNETCORE_ENVIRONMENT: "Production"
